import React from "react";
import ReactDOM from "react-dom/client";
import LosTragediasApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LosTragediasApp />
  </React.StrictMode>
);
